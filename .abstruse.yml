image: python2.7_builder

matrix:
  - env: SCRIPT='tests/python.sh'
  - env: SCRIPT='tests/javascript.sh'

before_script:
  - pip install -r requirements.txt

script:
  - if [[ "$SCRIPT" ]]; then ./$SCRIPT; fi

cache: