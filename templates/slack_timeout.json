{
  "channel": "{{ app.get('slack.channel') }}",
  "username": "{{ app.get('door.name') }}",
  "icon_emoji": ":door:",
  "link_names": 1,
  "attachments": [
    {
      "footer": "{{ text }}",
      "color": "#7D3C98",
      "fallback": "{{ text }}"
    }
  ]
}