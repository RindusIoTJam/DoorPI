{
  "channel": "{{ app.get('slack.channel') }}",
  "username": "{{ app.get('door.name') }}",
  "icon_emoji": ":door:",
  "link_names": 1,
  "attachments": [
    {
      "footer": "{{ text }}",
      "color": "#3aa3e3",
      "fallback": "{{ app.get('slack.baseurl') }}/slack/{{ secret }}",
    }
  ]
}